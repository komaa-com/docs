---
title: "Installation"
description: "Install hermes-plugin-teams-voice into a Hermes Agent environment."
keywords: ["hermes-plugin-teams-voice","Hermes Agent plugin","pip install Teams voice","teams_voice","uv pip"]
---

[`hermes-plugin-teams-voice`](https://pypi.org/project/hermes-plugin-teams-voice/) (plugin name **`teams_voice`**) is the Python half of the Teams CVI for
**Hermes Agent**. It hosts the HMAC WebSocket that StandIn connects to, runs the dialogue +
perception, and emits avatar driver cues. Chat-plane features live in the existing Teams adapter.

<Note>
  This adds **voice/video (CVI)** on top of Hermes Agent's existing
  [Teams messaging](https://hermes-agent.nousresearch.com/docs/user-guide/messaging/teams) - run both
  for chat + calls.
</Note>

## Requirements

- A working **Hermes Agent** install (the host).
- A **StandIn** subscription ([standin.komaa.com](https://standin.komaa.com), free package) - the
  hosted media bridge connecting to this plugin. See [Architecture](/concepts/architecture).
- **Realtime mode:** an OpenAI / Azure OpenAI realtime key. **Streaming mode:** needs **`ffmpeg`** on
  `PATH`.

## Install

Install into the **same Python environment as Hermes** so the `hermes_agent.plugins` entry-point is
discovered:

```bash
uv pip install --python /path/to/hermes/venv/bin/python hermes-plugin-teams-voice
```

Or with `pip`:

```bash
pip install hermes-plugin-teams-voice
```

Then enable the plugin:

```bash
hermes plugins enable teams_voice
```

## Verify

Show config + readiness:

```bash
hermes teams-voice status
```

Run the bridge server (foreground):

```bash
hermes teams-voice serve
```

The bridge binds its media WebSocket (default `ws://<host>:8443/voice/msteams/stream`).

## Expose the WebSocket (AGENT DOMAIN)

StandIn connects to your plugin **from the internet**, so port `8443` must be reachable - a public
host, or a tunnel. With Tailscale Funnel:

```bash
tailscale funnel --bg 8443
```

Your AGENT DOMAIN is then:

```
wss://<machine>.<tailnet>.ts.net/voice/msteams/stream
```

Register that URL with a **matching shared secret** in the
[StandIn sandbox](https://standin.komaa.com/sandbox), then place a Teams call to your bot.

<Note>
  Also running the **OpenClaw** plugin on the same machine? Funnel serves up to 3 external ports
  (443, 8443, 10000) - keep Hermes on 443 and put OpenClaw on another slot:
  `tailscale funnel --bg --https=8443 9442` → `wss://<machine>.<tailnet>.ts.net:8443/voice/msteams/stream`.
  Register each runtime as its own agent, each with its own secret.
</Note>

<Note>
  Next: the full [configuration reference](/hermes/configuration) - config.yaml + `.env`, realtime
  backend, and Graph permissions.
</Note>

## Links

<CardGroup cols={3}>
  <Card title="Source" icon="github" href="https://github.com/komaa-com/hermes-plugin-teams-voice" />
  <Card title="PyPI" icon="python" href="https://pypi.org/project/hermes-plugin-teams-voice/" />
  <Card title="Configuration" icon="gear" href="/hermes/configuration" />
</CardGroup>
